import React,{createContext,useContext,useState} from "react";import "./App.css";const Theme=createContext();const useT=()=>useContext(Theme);const Navbar=()=>{const {dark}=useT();return <nav className="card">Navbar ({dark?"Dark":"Light"})</nav>};const Card=()=>{const {dark}=useT();return <div className="card">Content Card - {dark?"Dark":"Light"} Mode</div>};const Footer=()=>{const {dark}=useT();return <footer className="card">Footer ({dark?"Dark":"Light"})</footer>};export default function App(){const [dark,setDark]=useState(false);return <Theme.Provider value={{dark}}><div className={dark?"container dark":"container"}><h1>Theme Switcher</h1><button onClick={()=>setDark(!dark)}>Toggle Theme</button><Navbar/><Card/><Footer/></div></Theme.Provider>}
