import React,{useState} from "react";import "./App.css";const shade=(hex,p)=>{let n=parseInt(hex.slice(1),16),r=n>>16,g=n>>8&255,b=n&255;r=Math.min(255,Math.floor(r+(255-r)*p));g=Math.min(255,Math.floor(g+(255-g)*p));b=Math.min(255,Math.floor(b+(255-b)*p));return `#${((1<<24)+(r<<16)+(g<<8)+b).toString(16).slice(1)}`};export default function App(){const [hex,setHex]=useState("#3498db");const colors=[0,.15,.3,.45,.6].map(p=>shade(hex,p));return <div className="container"><h1>Color Palette Generator</h1><input value={hex} onChange={e=>setHex(e.target.value)} /><div>{colors.map(c=><div key={c} className="card" style={{background:c}}>{c}</div>)}</div></div>}
