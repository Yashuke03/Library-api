import React,{useEffect,useState} from "react";import "./App.css";export default function App(){const [u,setU]=useState(null);const load=()=>fetch("https://randomuser.me/api/").then(r=>r.json()).then(d=>setU(d.results[0]));useEffect(load,[]);if(!u)return <div className="container">Loading...</div>;return <div className="container"><h1>Random User Card</h1><div className="card"><img alt="u" src={u.picture.large}/><h3>{u.name.first} {u.name.last}</h3><p>{u.email}</p><p>{u.location.city}, {u.location.country}</p></div><button onClick={load}>New User</button></div>}
