import React,{useState} from "react";import "./App.css";export default function App(){const [cat,setCat]=useState("Programming"),[joke,setJoke]=useState(""),[loading,setLoading]=useState(false);const get=()=>{setLoading(true);fetch(`https://v2.jokeapi.dev/joke/${cat}?type=single`).then(r=>r.json()).then(d=>{setJoke(d.joke||"No joke");setLoading(false)})};return <div className="container"><h1>Random Joke Generator</h1><select value={cat} onChange={e=>setCat(e.target.value)}><option>Programming</option><option>Misc</option><option>Pun</option></select><button onClick={get}>Get Joke</button>{loading?<p>Loading...</p>:<div className="card">{joke}</div>}</div>}
